'use client';

import React from 'react';

// Simplified CustomCursor to avoid complex logic if CursorFollower is used.
// Or we just remove it from layout if not strictly needed.
export default function CustomCursor() {
  return null;
}
